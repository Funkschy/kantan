import "tree/bintree"

type Person struct {
    name: string
}

def test(): Person {
    return Person { name: "Test" };
}

def bintest(): bintree.Person {
    return bintree.Person { name: "Bintree Person", age: 100 };
}

def traverse(tree: *bintree.Tree): void {
    bintree.traverse(tree);
}
